#!/bin/sh

prog=`basename $0`

test -z "$1" && {
	echo "$prog: you should specify a directory to serve" >&2
	exit 1
}

test ! -d "$1" && {
	echo "$prog: you should specify a directory to serve" >&2
	exit 1
}

cd "$1"
git daemon --reuseaddr --base-path=. --export-all --verbose

